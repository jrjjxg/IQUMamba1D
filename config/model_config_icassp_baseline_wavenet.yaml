model_config:

  model_type: icassp_baseline_wavenet
  input_channels: 2
  # Official task: one known complex SOI, encoded as I/Q.
  num_classes: 2
  # Matches icassp2024rfchallenge/src/configs/wavenet.yml.
  residual_channels: 128
  residual_layers: 30
  dilation_cycle_length: 10
