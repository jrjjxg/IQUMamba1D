# Stage397: Stage394 plus Stage381's alternating complex-state BiMamba.
model_config:
  model_type: iqumamba_stage397_stage394_complex_bimamba
  input_channels: 2
  num_classes: 4
  n_stages: 4
  features_per_stage: [32, 64, 128, 256]
  kernel_sizes: [3, 3, 3, 3]
  strides: [1, 2, 2, 2]
  n_conv_per_stage: [2, 2, 2, 2]
  n_conv_per_stage_decoder: [2, 2, 1, 1]
  deep_supervision: false

  # Stage394: adaptive real UniRepLK delta at stages 0 and 2.
  rf_apply_stages: [0, 2]
  rf_residual_scale_init: 0.05
  rf_kernels: [9, 17]
  rf_ffn_factor: 2
  rf_layer_scale: 1.0e-6

  # Stage381 layout: independent complex-state BiMamba at stages 1 and 3.
  bimamba_apply_stages: [1, 3]
  bimamba_residual_scale_init: 1.0
  complex_state_d_state: 8
  complex_state_d_conv: 4
  complex_state_expand: 2
  complex_state_scan_checkpoint: true
  complex_state_scan_backend: auto
  complex_state_fusion_hidden: 64
